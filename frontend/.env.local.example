# Runtime local do frontend Next.js
# Copie este arquivo para frontend/.env.local e preencha os valores reais.
# O arquivo .env.local na raiz não é carregado pelo app quando ele roda
# a partir do workspace frontend.

# Modo local padrão: catálogo seed read-only sem depender de segredos.
# Para testar cadastro, login, interesses e pré-lances, troque para "postgres"
# e preencha DATABASE_URL com uma conexão real.
KRON_DATA_MODE=local-seed
DATABASE_URL=

# Para providers gerenciados como Supabase, Railway Postgres ou Neon, use "require".
# Em desenvolvimento local com Postgres na máquina, use "disable".
DATABASE_SSL_MODE=require

# Opcional: habilita cache público de catálogo/atividade e rate limiting via Redis.
# Se não existir, o app continua funcionando com fallback para Postgres/no-cache.
REDIS_URL=
# Alternativas aceitas quando o provedor expõe variáveis separadas ou URL pública.
# Em produção, prefira REDIS_URL privada quando disponível.
REDIS_PUBLIC_URL=
REDISHOST=
REDISPORT=
REDISUSER=
REDISPASSWORD=
REDIS_PASSWORD=
REDIS_TLS=false

# URL pública canônica.
# Em preview na Vercel, o projeto já usa VERCEL_URL como fallback.
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Acesso simples do admin do MVP.
ADMIN_USERNAME=admin
ADMIN_PASSWORD=troque-esta-senha

# Tokens opcionais de verificação quando existir domínio oficial.
GOOGLE_SITE_VERIFICATION=
BING_SITE_VERIFICATION=

# Opcional: usado somente por npm run smoke:url quando a URL não for
# passada por argumento.
SMOKE_BASE_URL=
